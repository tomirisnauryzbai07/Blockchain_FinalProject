{
  "name": "finaltomi-blockchain",
  "version": "0.1.0",
  "private": true,
  "description": "BChT2 final project scaffold for an on-chain prediction market protocol",
  "scripts": {
    "build:contracts": "forge build",
    "test:contracts": "forge test -vvv",
    "fmt:contracts": "forge fmt",
    "fmt:check": "forge fmt --check"
  }
}

